<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">
    <!-- Permisos necesarios para que la clase PackageManager devuelva las apps instaladas que son visibles -->
    <queries>
        <intent>
            <action android:name="android.intent.action.MAIN" />

            <category android:name="android.intent.category.LAUNCHER" />
        </intent>
    </queries>

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.CredencialManger">
        <activity
            android:name=".presentacion.CredentialEntrega"
            android:exported="false" />
        <activity
            android:name=".noHaceNadaSoloRedirige"
            android:exported="false" />
        <activity
            android:name=".presentacion.MostrarCredenciales"
            android:exported="false" />
        <activity
            android:name=".presentacion.FormularioCredencial"
            android:exported="false" />
        <activity
            android:name=".presentacion.AgregarCredencialActivity"
            android:exported="false" />
        <activity
            android:name=".presentacion.RegistroUsuario"
            android:exported="false" />
        <activity
            android:name=".presentacion.Login"
            android:exported="false" />
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <service
            android:name=".negocio.MyCredentialProviderService"
            android:enabled="true"
            android:exported="true"
            android:icon="@mipmap/ic_launcher"
            android:label="Gestor de contraseñas"
            android:permission="android.permission.BIND_CREDENTIAL_PROVIDER_SERVICE">
            <intent-filter>
                <action android:name="android.service.credentials.CredentialProviderService" />
            </intent-filter>

            <meta-data
                android:name="android.credentials.provider"
                android:resource="@xml/provider" />
        </service>
    </application>

</manifest>